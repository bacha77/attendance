import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.philadelphie.sabbathschool',
  appName: ' Sabbath_School',
  webDir: 'dist'
};

export default config;
